# X-frame will be modelled as two arms (simplified as beams) crossing eachother. Forces are applied at tips and deconstructed into components parallel and perpendicular to the arms.
import math
import numpy as np

L = 0.03 # m